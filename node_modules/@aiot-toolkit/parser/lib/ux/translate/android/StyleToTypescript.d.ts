import IOffset from '../../../interface/IOffset';
import ITranslate from '../../../interface/ITranslate';
import IOptions from '../../config/IOptions';
import { IStyleAst } from '../../interface/IStyleAst';
import ITranslateOption from '../../interface/ITranslateOption';
import { AndroidFontFace, AndroidKeyFrames, AndroidMedia, AndroidStyle } from '../../interface/androidUx/IAndroidStyle';
/**
 * StyleToTypescript
 */
declare class StyleToTypescript implements ITranslate<IStyleAst, AndroidStyle> {
    options: IOptions;
    readonly compilerOption: ITranslateOption;
    constructor(options: IOptions, compilerOption: ITranslateOption);
    translate(sourceTree: IStyleAst, offsetList: IOffset[]): {
        targetTree: Partial<import("../../interface/androidUx/IAndroidStyle").AndroidStyleItem & AndroidFontFace & AndroidMedia & AndroidKeyFrames>;
        mapList: never[];
    };
    /**
     * 转换节点列表
     *
     * 1. 对于任一节点，根据类型做不同的转换
     * 2. 转换结果和已有的结果进行合并
     * @param nodes
     * @returns
     */
    private translateNodes;
    /**
     * 合并样式
     *
     * 1. media: 合并数组
     * 2. 其它（fontface, keyframes,  普通样式）：合并对象
     * @param currentStyle
     * @param newStyle
     * @returns
     */
    private mergeStyle;
    /**
     * 转换@ 开头的样式
     *
     * @param node
     * @returns
     */
    private translateAtRule;
    /**
     * 转换keyframe
     * @param node
     * @returns
     */
    private translateKeyFrames;
    /**
     * 转换  @font-face
     *
     * 1. 以font-family 为key
     * 2. 额外生成fontName=fontFamily   fontSrc=src
     *
     * @param node
     * @returns
     */
    private translateFontFace;
    /**
     * 转换 @media
     *
     * 1. 以"@MEDIA"为键
     * 2. 以条件为 `condition` 的值
     * 3. 递归 translateNodes 转换其中的内容
     * @param node
     * @returns
     */
    private translateMedia;
    /**
     * 转换常规样式
     * ```
     * .a .b {
     *   color: red;
     * }
     * ```
     * @param node
     * @returns
     */
    private translateRule;
    private translateCssValue;
    private getPreludeName;
}
export default StyleToTypescript;
