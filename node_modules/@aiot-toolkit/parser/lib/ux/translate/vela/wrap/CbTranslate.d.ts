import { Element } from 'aiot-parse5/dist/tree-adapters/default';
import IOptions from '../../../config/IOptions';
import IAttributeExtractionInformation from '../../../interface/IAttributeExtractionInformation';
import IWrapTranslate from './IWrapTranslate';
import ITranslateOption from '../../../interface/ITranslateOption';
/**
 * cb 函数生成
 *
 * 当满条件时生成
 * 1. 是 block 节点
 *    1. 如果是根元素，生成普通cb 函数
 *    2. 如果"有static 属性" 或 "同时有dynamic/data"，生成 opts 包含 dynamic， data 的 cb 函数
 *
 */
declare class CbTranslate implements IWrapTranslate {
    readonly options: IOptions;
    readonly compilerOption: ITranslateOption;
    extractionInformation: IAttributeExtractionInformation;
    private templateUtil;
    constructor(options: IOptions, compilerOption: ITranslateOption, extractionInformation: IAttributeExtractionInformation);
    match(node: Element): boolean;
    translate(node: Element, childrenExpression: string, identifiers: string[], extraParams?: string): Promise<string>;
}
export default CbTranslate;
