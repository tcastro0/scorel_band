import { SourceFile } from 'ts-morph';
import IOffset from '../../../interface/IOffset';
import ITranslate from '../../../interface/ITranslate';
import IOptions from '../../config/IOptions';
import { IScriptAst } from '../../interface/IScriptAst';
import ITranslateOption from '../../interface/ITranslateOption';
import { IFileLaneContext } from 'file-lane';
/**
 * ScriptToTypescript
 */
declare class ScriptToTypescript implements ITranslate<IScriptAst, SourceFile> {
    options: IOptions;
    readonly compilerOption: ITranslateOption;
    readonly context: IFileLaneContext;
    private _systemFeatures;
    constructor(options: IOptions, compilerOption: ITranslateOption, context: IFileLaneContext);
    translate(sourceTree: IScriptAst, offsetList: IOffset[]): Promise<{
        targetTree: SourceFile;
        mapList: never[];
    }>;
    /**
     * babel自定义插件实现替换
     * @param sourceCode
     * @returns
     */
    private babelScript;
    /**
     * 缓存并检查 feature
     */
    private cacheAndCheckFeature;
    /**
     * 检查代码
     */
    private checkCode;
}
export default ScriptToTypescript;
