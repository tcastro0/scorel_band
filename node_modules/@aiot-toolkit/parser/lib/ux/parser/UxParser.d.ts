import { Dictionary } from '@aiot-toolkit/shared-utils';
import IParser, { ParserResult } from '../../interface/IParser';
import IOptions from '../config/IOptions';
import { IImageFunction } from '../interface/IImageResource';
import ITranslateOption from '../interface/ITranslateOption';
import IUxAst from '../interface/IUxAst';
/**
 * UxParser
 */
declare class UxParser implements IParser<IUxAst> {
    options: IOptions;
    readonly compilerOption: ITranslateOption;
    globalVar: Dictionary<string>;
    collectImageResource: IImageFunction;
    constructor(options: IOptions, compilerOption: ITranslateOption, globalVar: Dictionary<string>, collectImageResource: IImageFunction);
    parser(): Promise<ParserResult<IUxAst>>;
    parserSyncWithoutStyle(): ParserResult<IUxAst>;
    /**
     * 验证内容
     * 1. template：
     *    a. 最多1个
     *    b. 子元素有且仅有1个，不能是 block
     */
    private validateContent;
    /**
     * 解析import内容
     * @param sourceNode
     * @param ast
     */
    private parserImport;
}
export default UxParser;
