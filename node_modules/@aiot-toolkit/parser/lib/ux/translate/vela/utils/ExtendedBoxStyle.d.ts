import { BlockItem, StyleItem } from '../../../interface/IStyleAst';
/**
 * ExtendedBoxStyle
 */
declare class ExtendedBoxStyle {
    static extendBoxStyle(sourceNode: BlockItem): StyleItem;
    /**
     * 扩展border样式
     * 扩展border的前提是border的格式符合CSS标准
     * 1. 展开border节点为borderWidth、borderStyle、borderColor
     * 2. 扩展borderWidth、borderColor属性
     * 3. 将border[Left、Right、Top、Bottom]由第1步生成的结果赋值给targetList
     * @param sourceNode
     * @param targetList
     */
    private static extendBorderStyle;
    /**
     * 扩展元素与位置
     * margin->0：'marginTop', 1：'marginRight', 2：'marginBottom', 3：'marginLeft'
     * 解析时，只有valueList.length > 1才会返回列表
     * 1. 节点值为数组且长度大于1时，待扩展值顺序为数组值的顺序
     * 2. 若待扩展元素无对应数组值，则取对称位置的数组值index-2
     * 3. 节点值为非数组，则取对应节点值即可
     * @param sourceNode
     * @param targetList
     */
    private static extendPosition;
    /**
     * 按照大写字符拆分字符串
     * bordeWidth -> border,width
     * @param str
     * @returns
     */
    private static splitByUppercase;
    /**
     * 展开border的每个值
     * @param sourceNode
     * @param extendedBorder
     */
    private static extendValueTypeNode;
    /**
     * 判断border节点值是否有borderWidth
     * @param nodeValue
     * @returns
     */
    private static isBorderWidth;
    /**
     * 第一个时间为 transitionDuration
     * @param sourceNode
     * @param targetList
     */
    private static extendTransition;
}
export default ExtendedBoxStyle;
