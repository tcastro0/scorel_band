import { Dictionary } from '@aiot-toolkit/shared-utils';
import IOptions from '../../../config/IOptions';
import { IAttributeNode } from '../../../interface/IIdentifierNode';
import ITemplateValueResult from '../../../interface/ITemplateValueResult';
import ITranslateOption from '../../../interface/ITranslateOption';
declare const ATTRIBUTE_CONFIG: Dictionary<{
    aliasName?: string;
    translate?: (value: ITemplateValueResult, option: IOptions, compilerOption: ITranslateOption, attribute: IAttributeNode) => string | Promise<string>;
}>;
/**
 * 转换动态值的默认方法
 * @param value
 * @param disabledWrap 是否禁用函数包裹。因为普通属性不用函数无法动态响应，而 for 的 list 属性设置函数又报错；所以这里临时加一个参数，在生成__list__的地方手动设置为 true
 * @returns
 */
declare const defaultAttributeTranslate: (value: string, disabledWrap?: boolean) => string;
/**
 * 转换事件属性的值
 *
 * 1. 普通函数不转换: function(){}
 * 2. 函数名：_vm_.a  转换为 _vm_.a(event)
 * 3. 函数调用：_vm_.a(x, y)  转换为 _vm_.a(x, y, event)
 * 4. 箭头函数不转换: ()=>{}
 *
 * @param value
 * @returns
 */
declare const eventAttributeTranslate: (options: IOptions, value: string, attribute: IAttributeNode) => string;
export { defaultAttributeTranslate, eventAttributeTranslate };
export default ATTRIBUTE_CONFIG;
