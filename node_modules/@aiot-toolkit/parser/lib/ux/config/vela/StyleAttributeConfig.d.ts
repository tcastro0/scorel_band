import { Dictionary, Loglevel } from '@aiot-toolkit/shared-utils';
import { StyleMessage } from '@aiot-toolkit/shared-utils';
import { BlockItem, StyleItem } from '../../interface/IStyleAst';
type ValidateInfo = {
    level: Loglevel;
    message: StyleMessage[];
};
type ConfigType = {
    translate?: (sourceNode: BlockItem) => StyleItem;
    validate?: (sourceNode: BlockItem) => ValidateInfo;
};
declare const STYLE_ATTRIBUTE_CONFIG: Dictionary<ConfigType>;
declare const STYLE_ATTRIBUTE_LIST: string[];
export { ConfigType, STYLE_ATTRIBUTE_CONFIG, STYLE_ATTRIBUTE_LIST, ValidateInfo };
