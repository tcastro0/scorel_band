import Koa from 'koa';
import { default as Router } from 'koa-router';
import { Server } from 'net';
import IRouter from '../interface/IRouter';
/**
 * NetworkUtil
 */
declare class NetworkUtil {
    /**
     * clone git 项目
     * @param url
     * @param localPath
     * @param showProgress 是否显示进度条，默认为 true，会在命令行工具中显示绿色进度条
     * @returns
     */
    static gitClone(url: string, localPath: string, showProgress?: boolean): Promise<void>;
    /**
     * 创建 http 服务器
     * @param data
     * @returns
     */
    static createHttpServer(data: {
        /**
         * 期望的端口
         */
        wantPort?: number;
        /**
         * 路由列表
         */
        routeConfigList: IRouter[];
        staticFolder?: string;
        /**
         * 事件
         */
        events?: {
            onSuccess?: (port: number) => void;
        };
    }): Promise<{
        /**
         * sever实例
         */
        server: Server;
        /**
         * 实际使用的端口
         */
        port: number;
        app: Koa;
    }>;
    static getIPv4IPAddress(): string | undefined;
    /**
     * 从请求中获取客户端 ip
     * @param ctx koa的请求上下文
     * @returns
     */
    static getClientIp(ctx: Router.RouterContext): string | undefined;
    /**
     * 从网络地址中获取真实 ip
     *
     * @example
     * @param ip
     * @returns
     */
    static stripPrefixForIPV4MappedIPV6Address(ip: string): string;
}
export default NetworkUtil;
