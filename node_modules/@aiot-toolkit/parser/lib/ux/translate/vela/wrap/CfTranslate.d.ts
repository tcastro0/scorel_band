import { Element } from 'aiot-parse5/dist/tree-adapters/default';
import { ts } from 'ts-morph';
import IOptions from '../../../config/IOptions';
import { IAttributeNode } from '../../../interface/IIdentifierNode';
import IWrapTranslate from './IWrapTranslate';
import ITranslateOption from '../../../interface/ITranslateOption';
/**
 * CfTranslate
 */
declare class CfTranslate implements IWrapTranslate {
    readonly options: IOptions;
    readonly compilerOption: ITranslateOption;
    readonly KEY = "for";
    private templateUtil;
    constructor(options: IOptions, compilerOption: ITranslateOption);
    match(node: Element): boolean;
    translate(node: Element, childrenExpression: string, identifiers: string[], extraParams?: string): Promise<string>;
    private getModifiers;
    private genExp;
    /**
     * 从 for 节点摘取 ： index item list
     *
     * @example
     * (item, index) in data  摘取
     * itemNode=item
     * indexNode=index
     * listNode=data
     *
     * @param attribute
     * @param options
     * @returns
     */
    static extractForNodes(attribute: IAttributeNode, options: IOptions): {
        itemNode: ts.Identifier;
        indexNode: ts.Identifier;
        listNode?: ts.Node;
    };
}
export default CfTranslate;
