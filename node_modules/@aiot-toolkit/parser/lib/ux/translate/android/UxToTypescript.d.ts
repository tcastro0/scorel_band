import { SourceFile } from 'ts-morph';
import IOffset from '../../../interface/IOffset';
import ITranslate from '../../../interface/ITranslate';
import IOptions from '../../config/IOptions';
import IImportAst from '../../interface/IImportAst';
import ITranslateOption from '../../interface/ITranslateOption';
import IUxAst from '../../interface/IUxAst';
import { IFileLaneContext } from 'file-lane';
/**
 * UxToTypescript
 */
declare class UxToTypescript implements ITranslate<IUxAst, SourceFile> {
    readonly options: IOptions;
    readonly compilerOption: ITranslateOption;
    readonly contxt: IFileLaneContext;
    private contenAccess;
    constructor(options: IOptions, compilerOption: ITranslateOption, contxt: IFileLaneContext);
    /**
     *
     * 转换过程
     * 1. 源树分为 import  template  script  style 4部分
     * 2. 转换import: 此步骤使用 import
     * 3. 转换template: 此步骤使用 template
     * 4. 转换script: 此步骤使用 import  script
     * 5. 转换style: 此步骤使用 style
     * 6. 添加包裹函数
     * @param sourceTree
     * @param offsetList
     * @param param
     */
    translate(sourceTree: IUxAst, offsetList: IOffset[]): Promise<{
        targetTree: SourceFile;
        mapList: never[];
        sourceMap: import("source-map").SourceMapGenerator;
    }>;
    importToTypescript(importList: IImportAst[]): string[];
    private wrapUx;
    private wrapPageUx;
    private getToolVersion;
    private wrapCommonUx;
    private wrapAppUx;
    private ruleScript;
    private wrapScript;
}
export default UxToTypescript;
