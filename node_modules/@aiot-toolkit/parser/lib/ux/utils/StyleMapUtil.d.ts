import { CssLocation } from 'css-tree';
import { SourceMapConsumer } from 'source-map';
import IOptions from '../config/IOptions';
declare class StyleMapUtil {
    private readonly sourceMapConsumer;
    private readonly lineOffset;
    private readonly options;
    constructor(sourceMapConsumer: SourceMapConsumer, lineOffset: number, options: IOptions);
    /**
     * 1. 根据生成结果位置找转换前的行列值
     * 2. 根据样式代码总体偏移量，计算出当前节点行在ux中的行
     *  2.1 查看map信息，原始行总是从第三行开始计算，-2是去掉生成map信息时多余的偏移量
     *  2.2 再加上样式代码总体偏移量，就可以计算出实际所在行数
     * 3. 返回节点原始位置信息
     *  3.1 原始位置的startLine即为上一步计算了偏移量的行信息
     *  3.2 原始位置的startColumn为查找到的节点列信息（目前看结果不存在列偏移）
     *  3.3 原始位置的endLine为startLine+生成结果中所占据行数的值
     *  3.4 原始位置的endColumn根据map结果，发现存在1个位置的差异，所以-1
     *  3.5 source存储来源的文件
     * @param location 生成结果中节点的位置信息
     * @param consumer 经过解析器获取的map信息生成的consumer对象
     * @param offset 当前样式代码在ux文件中的偏移量
     * @returns
     */
    transfromLocToPosition(location: CssLocation): {
        pos: number;
        end: number;
        startLine: number;
        startColumn: number;
        endLine: number;
        endColumn: number;
        source: string;
    };
}
export default StyleMapUtil;
