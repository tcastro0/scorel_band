/**
 * 偏移数据结构。表示 **源码节点** 相对于 **目标代码节点** 的偏移位置
 *
 * @description
 * ```
 * // 例如，源码为
 * const a = 10
 * const b = 11
 *
 * // 转换成
 * var a = 10;
 * let b = 11
 *
 * // let 对应的
 * IOffset = {
 *   pos: 14              // let 的结束位置
 *   line: 1              // 第2个 const 所在的行
 *   column: 5,           // 第2个 const 所在的列
 *   currentOffset: -2,   // let 相对 const 小了2个长度
 *   offset: -4           // [let 和 var] 共造成偏移-4
 * }
 *
 * // 表示：转换后的代码中，大于14的节点的位置（例如 b)；在源码中的位置应该-4
 *
 * ```
 */
export default interface IOffset {
    /**
     * 偏移起始位置
     */
    pos: number;
    /**
     * 源节点的起始行
     */
    line: number;
    /**
     * 源节点的起始列
     */
    column: number;
    /**
     * 自身偏移量：仅包含当前节点引起的偏移量
     */
    currentOffset: number;
    /**
     * 累计偏移量：等于从0到当前节点所有的偏移量之和
     */
    offset: number;
}
