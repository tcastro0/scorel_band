import Archiver from 'archiver';
/**
 * ZipFileUtil
 */
declare class ZipFileUtil {
    /**
     * 创建zip文件
     * @param zipFilePath zip文件路径
     * @param fileList 压缩进 zip 的文件列表
     * @param options 压缩参数, 默认{ zlib: { level: 9 } }
     */
    static createZipFile(zipFilePath: string, fileList: {
        filePath: string;
        zipFilePath: string;
    }[], options?: Archiver.ArchiverOptions): Promise<void>;
}
export default ZipFileUtil;
