import IOffset from '../../interface/IOffset';
import IParser from '../../interface/IParser';
import IOptions from '../config/IOptions';
import { IScriptAst } from '../interface/IScriptAst';
/**
 * ScriptParser
 */
declare class ScriptParser implements IParser<IScriptAst> {
    private readonly options;
    constructor(options: IOptions);
    parser(content: string): {
        ast: IScriptAst;
        offsetList?: IOffset[] | undefined;
    };
}
export default ScriptParser;
