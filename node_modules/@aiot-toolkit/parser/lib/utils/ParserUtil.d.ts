import { Project, SourceFile } from 'ts-morph';
import IOffset from '../interface/IOffset';
/**
 * ParserUtil
 */
declare class ParserUtil {
    static project: Project;
    /**
     * 根据偏移列表，查找 pos 对应的偏移量
     * @param pos
     * @param offsetList
     * @returns
     */
    static getOffsetPos(pos: number, offsetList: IOffset[]): number;
    /**
     * 获取指定位置的列偏移量
     *
     * 偏移量= 从offsetList 中获取 (在同一行  且 位置小于 position ) 的项，偏移量进行累加。
     *
     * 确保同一行有多个偏移项也可获取正确的偏移量
     * @param position
     * @param offsetList
     */
    static getOffsetColumn(position: {
        line: number;
        pos: number;
    }, offsetList?: IOffset[]): number;
    static createSourceFile(source: string, fileName?: string): SourceFile;
    static createProject(): Project;
}
export default ParserUtil;
