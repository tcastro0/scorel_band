declare enum IRuleTypes {
    RULE = "Rule",
    AT_RULE = "Atrule"
}
declare enum Selector {
    CLASS_SELECTOR = "ClassSelector",
    ID_SELECTOR = "IdSelector",
    TYPE_SELECTOR = "TypeSelector",
    COMBINATOR = "Combinator",
    /**
     * 伪类选择符
     */
    PSEUDO_CLASS_SELECTOR = "PseudoClassSelector"
}
declare enum AtruleSelector {
    KEYFRAMES = "keyframes",
    MEDIA = "media",
    FONTFACE = "font-face"
}
declare enum MediaType {
    ALL = "all",
    PRINT = "print",
    SCREEN = "screen",
    SPEECH = "speech"
}
declare function findSelectorIndex(type: string): number;
declare function isMeidaType(type: MediaType): boolean;
/**
 * 安卓快应用支持所有的媒体类型，只需要判断媒体类型是否合法
 * vela快应用仅支持 MediaType.SCREEN
 * @param type
 * @param projectType
 * @returns
 */
declare function isSupportMediaType(type: string, projectType: string): boolean;
export { AtruleSelector, IRuleTypes, Selector, MediaType, findSelectorIndex, isMeidaType, isSupportMediaType };
