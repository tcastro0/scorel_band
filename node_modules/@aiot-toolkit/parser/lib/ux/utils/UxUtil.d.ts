import { Dictionary } from '@aiot-toolkit/shared-utils';
import { Element } from 'aiot-parse5/dist/tree-adapters/default';
import IImportAst from '../interface/IImportAst';
import ITranslateOption from '../interface/ITranslateOption';
/**
 * UxParserUtil
 */
declare class UxUtil {
    /**
     * 重组文件名
     * @param fileName
     * @param extValue
     * @returns
     */
    static spliceFileName(fileName: string, extValue: string): string;
    /**
     * 获取css的lang类型
     * @param node
     * @returns
     */
    static getLangType(node: Element): string;
    /**
     * 更新文件路径中的后缀
     * @param path
     * @param extValue
     * @returns
     */
    static updateFileName(path: string, extValue: string): string;
    /**
     * 将全局变量根据样式语言类型转为对应的变量声明
     * {
     *  "a": "#fff"
     * }
     * 转为
     * $a: #fff 或者 @a: #fff
     * @param globalVar
     * @param langType
     * @returns
     */
    static convertGlobalJson(globalVar: Dictionary<string>, langType?: string): string;
    /**
     * 是否是绝对资源路径
     *
     * 1. 以/开头：/a/b/c.png
     * 2. 以 ***://开头的视为绝对路径: package://a.png
     * @param path
     */
    static isAbsoluteResource(path?: string): boolean;
    static importToTypescript(importList: IImportAst[], compilerOption: ITranslateOption): string[];
}
export default UxUtil;
