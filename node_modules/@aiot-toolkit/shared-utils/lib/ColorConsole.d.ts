import { ILog, Loglevel } from './interface/ILog';
export interface IStyle {
    color?: string;
    bgColor?: string;
    bold?: boolean;
    italic?: boolean;
    icon?: string;
    underline?: boolean;
}
export type StyleMessage = string | number | boolean | undefined | {
    word: string | number;
    style?: IStyle;
};
/**
 * Log
 */
declare class ColorConsole {
    static defaultStyle: IStyle;
    static styleDic: {
        [key in Loglevel]: IStyle;
    };
    static getStyle(logLevel: Loglevel): IStyle;
    static log(...message: StyleMessage[]): void;
    static error(...message: StyleMessage[]): void;
    static info(...message: StyleMessage[]): void;
    static success(...message: StyleMessage[]): void;
    static throw(...message: StyleMessage[]): void;
    static warn(...message: StyleMessage[]): void;
    /**
     * 创建复杂格式的消息，可单独设置词语样式
     *
     * @example
     * ```js
     * createMessage("my name is", {message:'****', style:{ color:'red' }})
     * ```
     * @param words
     * @returns
     */
    static createMessage(words: StyleMessage | StyleMessage[]): string;
    private static _log;
    private static formatWord;
    /**
     * 按照日志类型打印不同颜色的日志
     * @param log 默认日志类型为info
     * @returns 返回打印的日志信息
     */
    static logger(log: ILog): void;
    /**
     * 日志转换为带颜色的字符串
     *
     * @param log 日志对象
     * @param prefix 前缀列表
     * false-不使用前缀
     * string[]-自定义前缀
     * 默认-[图标、时间、toolkit字样]
     *
     * @example logToString(log, ['myPrefix'])
     */
    static logToString(log: ILog, prefix?: string[] | false): string;
    /**
     * 创建默认消息前缀列表
     *
     * 1. 图标
     * 2. 时间
     * 3. toolkit 字样
     *
     * @param log
     */
    static createDefaultPrefix(log: ILog): string[];
}
export default ColorConsole;
