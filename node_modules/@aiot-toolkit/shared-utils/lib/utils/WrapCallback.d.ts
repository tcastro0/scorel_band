import { Logger } from '../interface/ILog';
/**
 * 包装回调函数
 */
declare class WrapCallback {
    /**
     * 包装日志回调函数
     * @param onLog
     * @returns
     */
    static wrapOnLog(onLog: Logger): Logger;
}
export default WrapCallback;
