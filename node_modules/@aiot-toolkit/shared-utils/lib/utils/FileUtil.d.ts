import Fs from 'fs-extra';
import { RimrafSyncOptions } from 'rimraf';
type OneMatchType = string | RegExp | ((path: string) => boolean);
export type MatchType = OneMatchType | OneMatchType[];
/**
 * FileUtil
 */
declare class FileUtil {
    /**
     * 判断字符串是否符合包含、不包含的条件
     *
     * 规则为：
     * 1. 如果设置了不包含且匹配，则不包含
     * 2. 否则，如果设置了包含且不匹配，则不包含
     * 3. 否则，包含
     * @param value
     * @param include
     * @param exclude
     */
    static include(path: string, include?: MatchType, exclude?: MatchType): boolean;
    /**
     * 判断字符串是否匹配指定规则
     * @param value
     * @param matchType
     * @returns
     */
    static match(value: string, matchType: MatchType): boolean;
    /**
     * 获取指定目录所有层级的文件
     * @param dir
     * @returns
     */
    static readAlldirSync(dir: string, includes?: MatchType, excludes?: MatchType): string[];
    /**
     * 替换文件内容
     *
     * @see replaceKeywords
     * @param filePath
     * @param keywordDic
     */
    static replaceFileContent(filePath: string, keywordDic: Record<string, string>): void;
    /**
     * 替换字符串内容
     *
     * @example
     * // 替换 abc{{value}} 为 abc123
     * replaceKeywords(`abc{{value}}`, {value: '123'})
     *
     * @param content
     * @param keywordDic
     * @returns
     */
    static replaceKeywords(content: string, keywordDic: Record<string, string>): string;
    /**
     * 更新文件路径
     * @param filePath
     * @param newFileName
     * @returns
     * @example updateFileName('./a/b/c.ux', 'd.js') 转换结果为'./a/b/d.js'
     */
    static updateFileName(filePath: string, newFileName: string): string;
    /**
     * 复制文件
     * @param sourceDir 源目录
     * @param targetDir 目标目录
     * @param excludes  不包含的文件
     * @param includes 包含的文件
     */
    static copyFiles(sourceDir: string, targetDir: string, excludes?: MatchType, includes?: MatchType): void;
    /**
     * 检查指定的文件列表是否全部存在
     * @param fileList 文件列表
     */
    static checkFilePath(fileList: string[]): boolean;
    static del(patterns: string | string[], options?: RimrafSyncOptions): Promise<boolean>;
    /**
     * 创建目录
     *
     * *支持 windows 系统创建隐藏目录
     * @param path
     * @param options
     * @returns
     */
    static mkdirSync(path: Fs.PathLike, options?: Fs.MakeDirectoryOptions & {
        /**
         * 是否为隐藏目录-windows系统有效
         */
        hidden: true;
    }): string | undefined;
}
export default FileUtil;
