import { Node, NodePath } from '@babel/core';
import * as t from '@babel/types';
/**
 * @see：node_modules/@vue/compiler-core/dist/compiler-core.cjs.prod.js的 walkIdentifiers函数
 * BabelUtil
 */
declare class BabelUtil {
    /**
     * 是否是对象的标识符
     *
     * 例如：  a = {}
     * @param node
     * @returns
     */
    static isObjectIdentifier(node: Node): boolean;
    /**
     * 是否是对象键的标识符
     *
     * 例如： {x: **}  中的 x
     * @param node
     * @param parent
     * @returns
     */
    static isObjectKeyIdentifier(node: Node, parent: Node): boolean;
    /**
     * 生成 babel的函数体
     * @param code
     * @returns
     */
    static generateFunctionBody(code: string): t.BlockStatement;
    /**
     * 插入或更新函数到对象
     *
     * @param functionName 函数名. 如果存在此函数，则把body插入到最后；否则，添加函数
     * @param body 新增的函数体
     * @param path 对象路径(babel的路径)
     */
    static insertOrUpdateFunctionToObject(functionName: string, body: t.BlockStatement, path: NodePath<t.ExportDefaultDeclaration>): void;
    /**
     * 插入import, 如果 `importName` 存在，则不执行操作
     *
     * @example
     * import router from '@system/router'
     *
     * @param importName import的名称, 例如：router
     * @param importPath import的路径, 例如：@system/router
     * @param param babel节点， 只有 `Program` 类型时才执行
     */
    static insertImportToProgram(importName: string, importPath: string, param: t.Node): void;
}
export default BabelUtil;
