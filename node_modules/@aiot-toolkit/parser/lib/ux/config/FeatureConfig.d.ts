/**
 * FeatureConfig
 */
/**
 * FeatureConfig
 */
declare class FeatureConfig {
    private SYSTEM;
    private SERVICE;
    private ANDROID;
    private HAP;
    private SYSTEM_FEATURE;
    private SERVICE_FEATURE;
    /**
     * 是否为系统模块
     *
     * 以特定名称开头的为系统模块
     *
     * @example isSystemModule("@system.route")
     * @param modulePath
     * @returns
     */
    isSystemModule(modulePath: string): boolean;
    /**
     * 是否为有效的系统模块
     *
     * 有效系统模块指：显式配置的模块
     *
     * @param modulePath
     * @returns
     */
    isEffectSystemModule(modulePath: string): boolean;
}
declare const _default: FeatureConfig;
export default _default;
