"use strict";

function _typeof(obj) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (obj) { return typeof obj; } : function (obj) { return obj && "function" == typeof Symbol && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; }, _typeof(obj); }

function _get() { if (typeof Reflect !== "undefined" && Reflect.get) { _get = Reflect.get.bind(); } else { _get = function _get(target, property, receiver) { var base = _superPropBase(target, property); if (!base) return; var desc = Object.getOwnPropertyDescriptor(base, property); if (desc.get) { return desc.get.call(arguments.length < 3 ? target : receiver); } return desc.value; }; } return _get.apply(this, arguments); }

function _superPropBase(object, property) { while (!Object.prototype.hasOwnProperty.call(object, property)) { object = _getPrototypeOf(object); if (object === null) break; } return object; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function"); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, writable: true, configurable: true } }); Object.defineProperty(subClass, "prototype", { writable: false }); if (superClass) _setPrototypeOf(subClass, superClass); }

function _setPrototypeOf(o, p) { _setPrototypeOf = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function _setPrototypeOf(o, p) { o.__proto__ = p; return o; }; return _setPrototypeOf(o, p); }

function _createSuper(Derived) { var hasNativeReflectConstruct = _isNativeReflectConstruct(); return function _createSuperInternal() { var Super = _getPrototypeOf(Derived), result; if (hasNativeReflectConstruct) { var NewTarget = _getPrototypeOf(this).constructor; result = Reflect.construct(Super, arguments, NewTarget); } else { result = Super.apply(this, arguments); } return _possibleConstructorReturn(this, result); }; }

function _possibleConstructorReturn(self, call) { if (call && (_typeof(call) === "object" || typeof call === "function")) { return call; } else if (call !== void 0) { throw new TypeError("Derived constructors may only return object or undefined"); } return _assertThisInitialized(self); }

function _assertThisInitialized(self) { if (self === void 0) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return self; }

function _isNativeReflectConstruct() { if (typeof Reflect === "undefined" || !Reflect.construct) return false; if (Reflect.construct.sham) return false; if (typeof Proxy === "function") return true; try { Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function () {})); return true; } catch (e) { return false; } }

function _getPrototypeOf(o) { _getPrototypeOf = Object.setPrototypeOf ? Object.getPrototypeOf.bind() : function _getPrototypeOf(o) { return o.__proto__ || Object.getPrototypeOf(o); }; return _getPrototypeOf(o); }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }

Object.defineProperty(exports, "__esModule", {
  value: true
});

var utils_1 = require("./utils");

function matches(pattern, importee) {
  if (pattern instanceof RegExp) {
    return pattern.test(importee);
  }

  if (importee.length < pattern.length) {
    return false;
  }

  if (importee === pattern) {
    return true;
  }

  return importee.startsWith(pattern + '/');
}

var LessAliasPlugin = /*#__PURE__*/function () {
  function LessAliasPlugin(options) {
    _classCallCheck(this, LessAliasPlugin);

    this.options = void 0;
    this.options = options;
  }

  _createClass(LessAliasPlugin, [{
    key: "install",
    value: function install(less, pluginManager) {
      var alias = (0, utils_1.parseAlias)(this.options.alias);

      function resolveId(filename) {
        if (!filename) {
          return null;
        } // First match is supposed to be the correct one


        var matchedEntry = alias.find(function (entry) {
          return matches(entry.find, filename);
        });

        if (!matchedEntry) {
          return filename;
        }

        var resolvedPath = filename.replace(matchedEntry.find, matchedEntry.replacement);
        return resolvedPath;
      }

      var AliasePlugin = /*#__PURE__*/function (_less$FileManager) {
        _inherits(AliasePlugin, _less$FileManager);

        var _super = _createSuper(AliasePlugin);

        function AliasePlugin() {
          _classCallCheck(this, AliasePlugin);

          return _super.apply(this, arguments);
        }

        _createClass(AliasePlugin, [{
          key: "loadFile",
          value: function loadFile(filename, currentDirectory, options, enviroment) {
            var resolved;

            try {
              resolved = resolveId(filename);
            } catch (error) {
              console.error(error);
            }

            if (!resolved) {
              var error = new Error("[less-plugin-alias]: '".concat(filename, "' not found."));
              console.error(error);
              throw error;
            }

            return _get(_getPrototypeOf(AliasePlugin.prototype), "loadFile", this).call(this, resolved, currentDirectory, options, enviroment);
          }
        }, {
          key: "loadFileSync",
          value: function loadFileSync(filename, currentDirectory, options, enviroment) {
            var resolved;

            try {
              resolved = resolveId(filename);
            } catch (error) {
              console.error(error);
            }

            if (!resolved) {
              var error = new Error("[less-plugin-alias]: '".concat(filename, "' not found."));
              console.error(error);
              throw error;
            }

            return _get(_getPrototypeOf(AliasePlugin.prototype), "loadFileSync", this).call(this, resolved, currentDirectory, options, enviroment);
          }
        }]);

        return AliasePlugin;
      }(less.FileManager);

      pluginManager.addFileManager(new AliasePlugin());
    }
  }]);

  return LessAliasPlugin;
}();

exports.default = LessAliasPlugin;