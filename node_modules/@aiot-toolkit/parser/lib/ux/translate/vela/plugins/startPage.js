"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = startPage;
var _url = _interopRequireDefault(require("url"));
var _BabelUtil = _interopRequireDefault(require("../../../utils/BabelUtil"));
function _interopRequireDefault(e) { return e && e.__esModule ? e : { default: e }; }
function startPage() {
  return {
    visitor: {
      ExportDefaultDeclaration(path, state) {
        const opts = state.opts;
        const options = opts.options;
        if (options.fileType === 'app') {
          console.log('app');
          const {
            startPage
          } = opts.compilerOption;
          if (startPage) {
            const url = _url.default.parse(startPage, true);
            _BabelUtil.default.insertImportToProgram('router', '@system.router', path.parent);
            _BabelUtil.default.insertOrUpdateFunctionToObject('onShow', _BabelUtil.default.generateFunctionBody(`
              	setTimeout(() => {
                  router.replace({
                    uri: "${url.pathname}",
                    params: ${JSON.stringify(url.query)},
                  });
                }, 0);
              `), path);
          }
        }
      }
    }
  };
}