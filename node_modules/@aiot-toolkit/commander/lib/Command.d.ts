import IProgram from './interface/IProgram';
declare class Command {
    /**
     * 注册命令行项目
     * @param programConfig 配置
     */
    static registeProgram(programConfig: IProgram): void;
    private static registeCommand;
    /**
     * 采集参数。  根据不同的参数类型使用不同的询问方式
     * @param param
     * @returns
     */
    private static collectParam;
}
export default Command;
