import { Dictionary } from '@aiot-toolkit/shared-utils';
import { Element } from 'aiot-parse5/dist/tree-adapters/default';
import IOptions from '../../../config/IOptions';
import IAttributeTranslate from './IAttributeTranslate';
import ITranslateOption from '../../../interface/ITranslateOption';
/**
 * ForTranslate
 */
declare class ForTranslate implements IAttributeTranslate {
    readonly options: IOptions;
    readonly compilerOption: ITranslateOption;
    readonly KEY = "for";
    private templateUtil;
    constructor(options: IOptions, compilerOption: ITranslateOption);
    match(node: Element): boolean;
    /**
     * 转换 for 节点
     * 1. 只有 list，生成
     * ```
     * { repeat:function }
     * ```
     * 2. 有index、 item中的任意一项，生成
     * ```json
     * repeat: {
     *    exp: function,
     *    key: index,
     *    value: item
     * }
     * ```
     * @param node
     */
    translate(node: Element): Promise<Dictionary | null>;
}
export default ForTranslate;
